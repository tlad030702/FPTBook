@{
    Layout = "DashBoard";
}
@{
    ViewData["Title"] = "Reset password";
}

<h1>Reset password</h1>
@if (User.Identity.IsAuthenticated && User.IsInRole("Administrator"))
{
    <table class="table table-hover">
        <thead>
            <tr>
                <th>
                    ID
                </th>
                <th>
                    Email
                </th>
                <th>
                    UserName
                </th>
                <th>
                    Action
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Id
                    </td>
                    <td>
                        @item.Email
                    </td>
                    <td>
                        @item.UserName
                    </td>
                    @*@for (int i = 0; i < ViewBag.Roles.Count; i++)
                    {
                        <td>
                            <a asp-controller="Admin" asp-action="Edit" class="action-icon" asp-route-id="@item.Id"> <i class="mdi mdi-square-edit-outline "></i></a>
                        </td>
                    }*@
                    <td>
                        <a asp-controller="Admin" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                        @*<a asp-controller="Admin" asp-action="Edit" class="action-icon" asp-route-id="@item.Id"> <i class="mdi mdi-square-edit-outline "></i></a>*@
                    </td>
                </tr>
            }

        </tbody>
    </table>
}


